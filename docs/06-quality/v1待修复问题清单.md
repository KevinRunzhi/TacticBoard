# V1 待修复问题清单

更新日期：2026-04-16  
来源：全量自动化验证 + 浏览器端到端手工流模拟 + Windows 打包前回归检查

## 使用规则

- 只记录已确认的问题、隐患和需要跟踪的缺口。
- 每修完一项，就把对应条目标记为“已关闭”，并补上修复提交、验证方式和剩余风险。
- 不把纯猜测、未复现问题或未来增强需求混进本清单。

## 当前结论

- V1 主链路已验证通过：工作台 -> 新建项目 -> 编辑 -> 保存 -> 返回工作台 -> 继续最近项目 -> 重新打开。
- PNG 导出已真实下载验证通过。
- GIF 导出已真实下载验证通过。
- 设置页默认值至少有一条真实持久化链路已验证通过。
- 参考底图浏览器导入已验证通过。
- 当前没有发现阻塞 V1 演示的未实现大功能。
- 当前仍有 1 项需要继续修复或收口的问题。

## 待修复问题

### 1. 页面标题和元信息曾使用旧占位名

- 编号：`FIX-001`
- 优先级：`P1`
- 状态：`已关闭`
- 影响：
  - 浏览器标签页标题错误。
  - 社交分享元信息错误。
  - 后续 Windows 桌面壳标题一致性受影响。
- 现象：
  - 浏览器标题曾显示 `Lovable App`。
- 代码定位：
  - `tactics-canvas-24/index.html`
- 修复提交：未提交
- 修改文件：
  - `tactics-canvas-24/index.html`
  - `tactics-canvas-24/src-tauri/tauri.conf.json`
  - `tactics-canvas-24/src/components/v2/TopNavV2.tsx`
  - `tactics-canvas-24/src/pages/SettingsV2.tsx`
- 验证方式：
  - `npm run build`
  - `npm run test`
  - `npm run lint`
  - 浏览器实际打开首页并检查标题
- 验证结果：
  - 浏览器标题已更新为 `TacticBoard战术板`
  - Web 元信息入口已更新
  - Tauri `productName` 和窗口标题已更新
  - 顶部品牌文案和设置页版本名已更新
- 剩余风险：
  - 旧构建产物或历史日志中仍可能保留旧名称，但源码入口已经统一。

### 2. 通用弹窗关闭文案曾为英文 `Close`

- 编号：`FIX-002`
- 优先级：`P2`
- 状态：`已关闭`
- 影响：
  - 中文界面存在明显语言不一致。
  - 导出弹窗和抽屉类组件观感不完整。
- 现象：
  - 导出弹窗右上角曾显示 `Close`。
- 代码定位：
  - `tactics-canvas-24/src/components/ui/dialog.tsx`
  - `tactics-canvas-24/src/components/ui/sheet.tsx`
- 修复提交：未提交
- 修改文件：
  - `tactics-canvas-24/src/components/ui/dialog.tsx`
  - `tactics-canvas-24/src/components/ui/sheet.tsx`
- 验证方式：
  - 源码检索 `Close` 残留
  - `npm run build`
  - `npm run test`
  - `npm run lint`
  - 浏览器打开导出弹窗并检查页面文案
- 验证结果：
  - 通用弹窗和抽屉关闭控件已统一为中文 `关闭`
  - 导出弹窗页面中不再出现英文 `Close`
  - 关闭控件具备明确 `aria-label`
- 剩余风险：
  - 其他非本次范围的英文文案若存在，需要在后续文案清理中单独处理。

### 3. 关键控件曾缺少稳定可访问名称

- 编号：`FIX-003`
- 优先级：`P2`
- 状态：`已关闭`
- 影响：
  - 自动化测试定位不稳定。
  - 无障碍支持不足。
  - 部分图标按钮行为不易被真实用户识别。
- 现象：
  - 导出弹窗内多组复选框只有视觉文案，没有稳定的控件级标签绑定。
  - 一些抽屉和图标按钮缺少明确 `aria-label` 或 `title`。
- 代码定位：
  - `tactics-canvas-24/src/components/tactics/ExportConfigDialog.tsx`
  - `tactics-canvas-24/src/components/tactics/MobilePropertiesDrawer.tsx`
  - `tactics-canvas-24/src/components/tactics/MobileStepsDrawer.tsx`
  - `tactics-canvas-24/src/components/tactics/TabletLeftDrawer.tsx`
  - `tactics-canvas-24/src/components/tactics/TabletRightDrawer.tsx`
- 修复提交：未提交
- 修改文件：
  - `tactics-canvas-24/src/components/tactics/ExportConfigDialog.tsx`
  - `tactics-canvas-24/src/components/tactics/MobilePropertiesDrawer.tsx`
  - `tactics-canvas-24/src/components/tactics/MobileStepsDrawer.tsx`
  - `tactics-canvas-24/src/components/tactics/TabletLeftDrawer.tsx`
  - `tactics-canvas-24/src/components/tactics/TabletRightDrawer.tsx`
  - `tactics-canvas-24/src/test/accessibility-labels.test.tsx`
- 验证方式：
  - `npx vitest run src/test/accessibility-labels.test.tsx src/test/export-config-dialog.test.tsx`
  - `npm run test`
  - `npm run build`
  - `npm run lint`
  - 浏览器检查导出弹窗复选框 `aria-label`
- 验证结果：
  - 导出弹窗 6 个复选框均有稳定 `aria-label`
  - 移动端和平板抽屉的关键纯图标按钮已补 `aria-label` / `title`
  - 全量测试通过，当前 `32` 个测试文件、`75` 个测试通过
- 剩余风险：
  - 其他未纳入本轮范围的纯图标按钮若后续新增，仍需按相同规则补名称。

### 4. React Router 未来版本 warning 持续存在

- 编号：`FIX-004`
- 优先级：`P3`
- 状态：`已关闭`
- 影响：
  - 当前功能不受阻。
  - 后续升级 React Router 时存在迁移风险。
- 现象：
  - 控制台曾持续出现 `v7_startTransition` 和 `v7_relativeSplatPath` 相关 warning。
- 代码定位：
  - `tactics-canvas-24/src/lib/platform.ts`
  - `tactics-canvas-24/src/test/dashboard-workbench-entry.test.tsx`
  - `tactics-canvas-24/src/test/editor-save-return.test.tsx`
  - `tactics-canvas-24/src/test/projects-entry-semantics.test.tsx`
- 修复提交：未提交
- 修改文件：
  - `tactics-canvas-24/src/lib/platform.ts`
  - `tactics-canvas-24/src/test/dashboard-workbench-entry.test.tsx`
  - `tactics-canvas-24/src/test/editor-save-return.test.tsx`
  - `tactics-canvas-24/src/test/projects-entry-semantics.test.tsx`
- 验证方式：
  - `npm run test`
  - `npm run build`
  - `npm run lint`
  - 浏览器控制台抓取工作台和编辑器页面日志
- 验证结果：
  - 应用入口 Router 已统一启用 React Router future flags
  - 直接使用 `MemoryRouter` 的测试已同步 future flags
  - 全量测试、构建、lint 验证通过
  - 浏览器控制台抓取结果中不再出现 Router future warning
- 剩余风险：
  - 后续如果新增直接使用 `MemoryRouter` 的测试，需要继续同步相同的 future flags。

### 5. GIF 导出存在 Canvas 读回性能 warning

- 编号：`FIX-005`
- 优先级：`P3`
- 状态：`已关闭`
- 影响：
  - 当前 GIF 导出成功。
  - 复杂项目下可能有性能退化。
- 现象：
  - 控制台出现 `Canvas2D: Multiple readback operations ... willReadFrequently` warning。
- 代码定位：
  - `tactics-canvas-24/src/lib/tactics-export.ts`
- 重现步骤：
  1. 进入编辑器。
  2. 创建至少两步。
  3. 执行 GIF 导出。
  4. 查看控制台。
- 预期结果：
  - 导出过程不出现明显可优化的 Canvas 读回警告，或将该风险明确收口。
- 验收标准：
  - 若调整渲染上下文配置后警告消失，记录验证结果。
  - 若暂不修复，需要明确这是性能优化项，不是功能缺陷。
- 修复提交：未提交
- 修改文件：
  - `tactics-canvas-24/src/lib/tactics-export.ts`
  - `tactics-canvas-24/src/test/tactics-export.test.ts`
- 验证方式：
  - `npx vitest run src/test/tactics-export.test.ts`
  - `npm run build`
  - 全量 `npm run test`
  - `npm run lint`
- 验证结果：
  - GIF 导出改为使用独立的读回 canvas，并在首次获取 2D context 时启用 `willReadFrequently`
  - 定向测试通过，已覆盖读回 canvas 初始化参数
  - 构建、全量测试和 lint 验证通过
- 剩余风险：
  - 这次收掉的是高频 `getImageData` 读回警告，不涉及 GIF 编码性能的更深层优化；极大项目下仍可能需要进一步性能调优。

## 建议修复顺序

1. 当前清单中的阻塞项已全部关闭；后续新增问题按相同格式追加

## 已验证通过但暂不列为缺陷

- 主链路保存与重开功能正常。
- PNG 导出可真实下载。
- GIF 导出可真实下载。
- 设置页默认球员样式可影响新建项目。
- 浏览器参考底图导入可用。

## 后续记录格式

每关闭一项时，补充以下字段：

- 修复提交：
- 修改文件：
- 验证方式：
- 验证结果：
- 剩余风险：
